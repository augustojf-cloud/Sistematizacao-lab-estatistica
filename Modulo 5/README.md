# Módulo 5 — Correlação e Regressão Linear

**Status:** parcialmente implementado.

As funções `regressao_linear_simples()` e `r_quadrado()` já existem e
estão testadas em `Modulo 1/statslocal.py`, mas ainda não foram
integradas na interface do Streamlit (`Modulo 0/app.py`).

Pasta criada antecipadamente para manter a organização do repositório
(issue #19) — os arquivos de interface deste módulo entram aqui quando
a integração for feita.
